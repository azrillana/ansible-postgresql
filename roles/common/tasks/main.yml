- name: Install aptitude
  apt:
    name: aptitude
    state: latest
    update_cache: true

- name: Install required packages
  apt:
    name:
      - wget
      - python3-psycopg2
      - acl
    state: latest

